# 版本与更新

- Windows 官方版启动约四秒后检查稳定通道，自动检查最多每 24 小时一次，也可在设置页手动检查。
- 可用时先显示版本、发行说明、包大小和更新类型，由用户决定下载；强制兼容性更新不可跳过。
- 支持范围内优先使用 Git 增量更新；跨度过大或增量不可用时下载完整 Setup。
- 下载内容必须通过清单签名、文件大小、SHA-256 和来源校验。新版启动健康检查失败时自动回滚。
- Android 官方版下载完整 APK，校验包名、versionCode、文件哈希与签名证书后交给系统安装界面。
